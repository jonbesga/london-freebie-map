extends layout

block head
    link(rel="stylesheet", href="css/style.css")
    //- <!-- Start of Async Drift Code -->
    script.
        !function() {
        var t;
        if (t = window.driftt = window.drift = window.driftt || [], !t.init) return t.invoked ? void (window.console && console.error && console.error("Drift snippet included twice.")) : (t.invoked = !0, 
        t.methods = [ "identify", "config", "track", "reset", "debug", "show", "ping", "page", "hide", "off", "on" ], 
        t.factory = function(e) {
            return function() {
            var n;
            return n = Array.prototype.slice.call(arguments), n.unshift(e), t.push(n), t;
            };
        }, t.methods.forEach(function(e) {
            t[e] = t.factory(e);
        }), t.load = function(t) {
            var e, n, o, i;
            e = 3e5, i = Math.ceil(new Date() / e) * e, o = document.createElement("script"), 
            o.type = "text/javascript", o.async = !0, o.crossorigin = "anonymous", o.src = "https://js.driftt.com/include/" + i + "/" + t + ".js", 
            n = document.getElementsByTagName("script")[0], n.parentNode.insertBefore(o, n);
        });
        }();
        drift.SNIPPET_VERSION = '0.3.1';
        drift.load('8zkpzas883vf');    
    //- <!-- End of Async Drift Code -->
    
block content
    nav.navbar.navbar-toggleable-md.navbar-inverse.bg-inverse.fixed-top
        button.navbar-toggler.navbar-toggler-right(type='button', data-toggle='collapse', data-target='#navbarsExampleDefault', aria-controls='navbarsExampleDefault', aria-expanded='false', aria-label='Toggle navigation')
            span.navbar-toggler-icon
        a.navbar-brand(href='#') London Freebie Map
        
        #navbarsExampleDefault.collapse.navbar-collapse
            ul.navbar-nav.mr-auto
                li.nav-item.active
                    a.nav-link(href='#') About 
                    span.sr-only (current)
                li.nav-item.dropdown
                    a#dropdown01.nav-link.dropdown-toggle(href='http://example.com', data-toggle='dropdown', aria-haspopup='true', aria-expanded='false') Dropdown
                    .dropdown-menu(aria-labelledby='dropdown01')
                        a.dropdown-item(href='#') Action
                        a.dropdown-item(href='#') Another action
                        a.dropdown-item(href='#') Something else here
            .my-2.my-lg-0
                button#add-place.btn.btn-outline-success.my-2.my-sm-0(type='submit') Add place
        

    #map
    #editMode(style='display: none;')
        h2#instructions Drag the map to place the marker of the center where you want to create the marker and then press the green flag to add the properties
        #centerMarker

        .button-container
            .button-subcontainer
                button#saveButton.add-place(type='submit')
                    span.add-place-icon.map-icon.map-icon-embassy


    script.
        const rows = !{rows}
    //- script.
    //-     $('.navbar-nav>li>a').on('click', function(){
    //-         $('.navbar-collapse').collapse('hide');
    //-     });

    //-     $('.my-2.my-lg-0>button').on('click', function(){
    //-         $('.navbar-collapse').collapse('hide');
    //-     });

    script(src="/js/map.js")
    script(src='https://maps.googleapis.com/maps/api/js?key=AIzaSyAAqYZLLYExydl0vHbSPtQevqBZTntwpMQ&callback=initMap', async='', defer='')
        



